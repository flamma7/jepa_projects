Super simple ViT implemented from scratch on CIFAR-10 dataset
Only one attention block with 6 heads, 1D positional encodings, and a linear output layer

Just enough to grab the concepts. Reached 44% accuracy which is above 10% (random guessing) so the tiny network learned something. Accuracy could substantially be improved by adding more attention blocks
